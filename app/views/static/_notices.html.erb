<% unless flash.empty? %>
	<div class="row">
		<div class="large-12 columns">
			<% flash.each do |key, value| %>
				<p class="content-item notice notice-<%= key %> text-center"><%= value %></p>
			<% end %>
		</div>
	</div>
<% end %>
